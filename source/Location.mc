/**
 * Location of the countdown circle.
 **/
class Location {

    var degree;
    var x;
    var y;

    function initialize(degree as Numeric, x as Numeric, y as Numeric) {
        self.degree = degree;
        self.x = x;
        self.y = y;
    }
}